# vrf-hs

Run `make archive` inside `./lib` before attempting to build the library.

# Note
The VRF code is code made available publicly by Google, and has not been written by me (except `lib/main.go`).
